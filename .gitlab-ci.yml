stages:
  - install
  - test
  - build

install:
  stage: install
  image: node:16-alpine
  script:
    - yarn
  artifacts:
    expire_in: 1 hour
    paths:
      - node_modules/

build:
  stage: build
  image: node:16-alpine
  script:
    - yarn build

test:
  stage: test
  image: node:16-alpine
  script:
    - yarn test
